services:
  http-headers-scanner:
    build:
      context: ..
      dockerfile: http-headers-scanner/Dockerfile
    image: ghcr.io/samma-io/detect-http-headers-scanner:latest
    volumes:
      - ./out:/out
    environment:
      - TARGET=example.com
      - HTTPS=False
      - PORT=80
      - TIMEOUT=5
      - SAMMA_IO_SCANNER=http-headers-scanner
      - SAMMA_IO_ID=1234
      - SAMMA_IO_TAGS=scanner
      - SAMMA_IO_JSON={}
      - WRITE_TO_FILE=true
      - PARSER=http-headers-scanner
      - NATS_ENABLED=False
      - NATS_URL=nats://localhost:4222
      - NATS_SUBJECT=samma-io.scan
  #filebeat:
  #  image: docker.elastic.co/beats/filebeat:7.15.0
  #  volumes:
  #    - ./filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml
  #    - ./out:/out
